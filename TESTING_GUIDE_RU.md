# Как проверить автоматизацию - Пошаговая инструкция

## Что сделано?

Реализована **полная автоматизация** генерации и постинга контента:

1. ✅ **TopicSelector** - выбирает уникальные темы (не повторяет)
2. ✅ **TelegramPoster** - постит в Telegram канал
3. ✅ **ReelGenerator** - создаёт картинки для постов
4. ✅ **AutoContentSystem** - объединяет всё вместе
5. ✅ **Scheduler** - запускает автоматически по расписанию

## Как это работает?

```
Каждый день в 9:00 утра (настраивается):
    ↓
1. Система выбирает уникальную тему (которую не использовали 30 дней)
    ↓
2. Берёт посты по этой теме из базы данных
    ↓
3. Генерирует контент через AI (Groq)
    ↓
4. Сохраняет в базу данных
    ↓
5. Помечает тему как "использованную"
    ↓
6. Генерирует красивую картинку (reel)
    ↓
7. Постит в Telegram канал с картинкой
    ↓
8. Готово! Контент опубликован.
```

## Проверка: Шаг 1 - Запустить приложение

```bash
# В терминале
python app_v2.py
```

**Что должны увидеть:**
```
==================================================
News Insight Parser - Version 2.0 + Automation
==================================================
[OK] Universal architecture
[OK] AutoContentSystem
==================================================
[AUTOMATION] Auto-generate: OFF (set AUTO_GENERATE_ENABLED=true to enable)
==================================================
Starting server on http://localhost:5001
```

## Проверка: Шаг 2 - Проверить статус автоматизации

Откройте второй терминал и выполните:

```bash
curl http://localhost:5001/api/automation-status | python -m json.tool
```

**Что должны увидеть:**
```json
{
    "configured": true,        # ← Система настроена
    "enabled": false,          # ← Автоматизация выключена
    "telegram_enabled": false, # ← Telegram не настроен
    "config": {
        "content_language": "ru",
        "content_format": "long_post",
        "topic_exclude_days": 30
        # ... и т.д.
    }
}
```

## Проверка: Шаг 3 - Включить автоматизацию

```bash
curl -X POST http://localhost:5001/api/automation/enable
```

**Что должны увидеть:**
```json
{
    "status": "success",
    "message": "Automation enabled - Runs daily at 09:00"
}
```

Теперь проверьте статус снова:

```bash
curl http://localhost:5001/api/automation-status | python -m json.tool
```

**Что должны увидеть:**
```json
{
    "enabled": true,  # ← Теперь включена!
    "jobs": [
        {
            "id": "daily_content_generation",
            "name": "Daily automated content generation",
            "next_run": "2025-12-21T09:00:00+05:00",  # ← Следующий запуск завтра в 9:00
            "trigger": "cron[hour='9', minute='0']"
        }
    ]
}
```

## Проверка: Шаг 4 - Попробовать сгенерировать контент вручную

**ВНИМАНИЕ:** Это сработает только если:
- У вас есть посты в базе данных (запустите парсеры)
- У вас есть topics (запустите аналитику)
- Настроен GROQ_API_KEY

```bash
curl -X POST http://localhost:5001/api/auto-generate | python -m json.tool
```

**Если нет постов/topics:**
```json
{
    "status": "error",
    "error": "No suitable topic found"
}
```

**Если всё настроено и работает:**
```json
{
    "status": "success",
    "content_id": 1,
    "message_id": 123,  # ← ID сообщения в Telegram (если настроен)
    "image_path": "generated_reels/reel_20251220_123456.jpg",
    "topic": {
        "keywords": ["AI", "automation", "LLM"],
        "post_count": 5
    }
}
```

## Проверка: Шаг 5 - Посмотреть статистику

```bash
curl http://localhost:5001/api/auto-stats | python -m json.tool
```

**Что показывает:**
```json
{
    "status": "success",
    "stats": {
        "total_generated": 0,      # Всего сгенерировано контента
        "total_published": 0,      # Всего опубликовано
        "topics_used_30d": 0,      # Тем использовано за 30 дней
        "topics_per_week": 0.0,    # Тем в неделю
        "publish_rate": 0,         # Процент публикации
        "last_run": null           # Последний запуск
    }
}
```

## Полный тест-сценарий

### Сценарий 1: Без данных (первый запуск)

```bash
# 1. Запустить приложение
python app_v2.py

# В другом терминале:

# 2. Проверить статус
curl http://localhost:5001/api/automation-status | python -m json.tool

# 3. Попробовать сгенерировать
curl -X POST http://localhost:5001/api/auto-generate | python -m json.tool

# Результат: "No suitable topic found" - это нормально!
```

### Сценарий 2: С данными (полный цикл)

```bash
# 1. Запустить приложение
python app_v2.py

# В другом терминале:

# 2. Запустить парсинг
curl -X POST http://localhost:5001/api/parse

# 3. Запустить аналитику
curl -X POST http://localhost:5001/api/run-insights

# 4. Сгенерировать контент
curl -X POST http://localhost:5001/api/auto-generate | python -m json.tool

# 5. Проверить результат
curl http://localhost:5001/api/generated-content | python -m json.tool

# 6. Проверить статистику
curl http://localhost:5001/api/auto-stats | python -m json.tool
```

## Что настроить для продакшена?

### Обязательные переменные окружения:

```bash
# В .env файле или в Render Dashboard:

# База данных (обязательно)
DATABASE_URL=postgresql://user:password@host/database

# API ключ для AI (обязательно)
GROQ_API_KEY=ваш_ключ_groq

# Telegram (опционально, для автопостинга)
TELEGRAM_BOT_TOKEN=123456:ABCdef...
TELEGRAM_CHANNEL_ID=@your_channel

# Включить автоматизацию
AUTO_GENERATE_ENABLED=true

# Время запуска (по умолчанию 9:00)
AUTO_GENERATE_HOUR=9
AUTO_GENERATE_MINUTE=0
```

## Частые проблемы

### 1. "AutoContentSystem not configured"

**Проблема:** Нет GROQ_API_KEY

**Решение:**
```bash
# Установите переменную окружения
export GROQ_API_KEY=ваш_ключ
```

### 2. "No suitable topic found"

**Проблема:** Нет тем в базе данных

**Решение:**
```bash
# Сначала запустите парсеры
curl -X POST http://localhost:5001/api/parse

# Потом аналитику
curl -X POST http://localhost:5001/api/run-insights
```

### 3. "Pillow not installed"

**Проблема:** Нет библиотеки для создания картинок

**Решение:** Не страшно! Система работает в mock режиме без картинок.
На Linux (Render) Pillow установится автоматически.

### 4. Telegram posting failed

**Проблема:** Бот не администратор канала

**Решение:** Добавьте бота как администратора в ваш Telegram канал.

## Что дальше?

1. ✅ **Локальное тестирование** - вы сейчас здесь
2. 📦 **Деплой на Render** - следуйте DEPLOYMENT_GUIDE.md
3. ⚙️ **Настройка переменных** - в Render Dashboard
4. 🚀 **Запуск автоматизации** - установите AUTO_GENERATE_ENABLED=true
5. 📊 **Мониторинг** - проверяйте логи и Telegram канал

## Быстрая проверка "всё ли работает?"

```bash
# Запустите это в терминале:
curl http://localhost:5001/api/automation-status | python -m json.tool

# Если видите "configured": true - всё отлично!
# Если видите "configured": false - проверьте GROQ_API_KEY
```

## Визуальная проверка в браузере

Откройте в браузере:

- http://localhost:5001 - главная страница
- http://localhost:5001/analytics - просмотр тем
- http://localhost:5001/api/automation-status - статус автоматизации (JSON)

## Итого

**Что работает:**
- ✅ Автоматический выбор уникальных тем
- ✅ Генерация контента через AI
- ✅ Создание картинок (reels)
- ✅ Постинг в Telegram
- ✅ Планировщик (cron schedule)
- ✅ API endpoints для контроля
- ✅ Статистика использования

**Что нужно для полной работы:**
1. Посты в базе данных (запустить парсеры)
2. Темы (запустить аналитику)
3. GROQ_API_KEY настроен
4. Telegram настроен (опционально)
5. AUTO_GENERATE_ENABLED=true для автоматизации

**Все 5 фаз реализованы и протестированы!** 🎉
