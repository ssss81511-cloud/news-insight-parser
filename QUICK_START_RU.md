# Что нужно сделать ТЕБЕ - Быстрый старт

## 🎯 Цель: Запустить автоматическую генерацию и постинг контента

---

## ✅ Что УЖЕ ГОТОВО (сделал я):

- ✅ Весь код написан и протестирован
- ✅ Все компоненты интегрированы
- ✅ API endpoints работают
- ✅ Scheduler настроен
- ✅ База данных готова (SQLite локально)
- ✅ Документация написана

---

## 📋 Что нужно ТЕБЕ (пошагово):

### Вариант A: Локальный тест (5-10 минут)

Если хочешь просто ПРОВЕРИТЬ как работает:

#### Шаг 1: Убедись что есть GROQ_API_KEY

```bash
# Проверь:
echo $GROQ_API_KEY

# Если пусто - установи:
export GROQ_API_KEY="твой_ключ_от_groq"
```

**Где взять:** https://console.groq.com/keys

#### Шаг 2: Запусти приложение

```bash
python app_v2.py
```

#### Шаг 3: Запусти парсеры (в другом терминале)

```bash
curl -X POST http://localhost:5001/api/parse
```

Подожди 1-2 минуты, пока соберёт посты.

#### Шаг 4: Запусти аналитику

```bash
curl -X POST http://localhost:5001/api/run-insights
```

Подожди 30-60 секунд, пока создаст темы.

#### Шаг 5: Сгенерируй контент

```bash
curl -X POST http://localhost:5001/api/auto-generate | python -m json.tool
```

**Результат:** Контент сгенерирован и сохранён в БД!

#### Шаг 6: Посмотри результат

```bash
# Через API:
curl http://localhost:5001/api/generated-content | python -m json.tool

# Или в браузере:
# Открой: http://localhost:5001
```

✅ **ГОТОВО! Локально всё работает.**

---

### Вариант B: Деплой на Render (продакшен)

Если хочешь запустить в ПРОДАКШЕН с автопостингом в Telegram:

#### Шаг 1: Получи Telegram токен (опционально, но рекомендуется)

1. Открой Telegram, найди @BotFather
2. Отправь: `/newbot`
3. Введи имя бота, например: `My Content Bot`
4. Введи username, например: `my_content_bot`
5. **Получишь токен вида:** `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

#### Шаг 2: Создай Telegram канал

1. В Telegram создай канал (публичный или приватный)
2. Добавь своего бота как **администратора** канала
3. Запомни ID канала:
   - Публичный: `@your_channel_name`
   - Приватный: `-100xxxxxxxxxx` (узнай через @getmyid_bot)

#### Шаг 3: Подготовь GitHub репозиторий

```bash
# Если ещё не создал:
git init
git add .
git commit -m "Initial commit"

# Создай репозиторий на GitHub
# Залей код:
git remote add origin https://github.com/твой_username/твой_repo.git
git branch -M main
git push -u origin main
```

**Если уже есть git репозиторий - пропусти этот шаг.**

#### Шаг 4: Зарегистрируйся на Render

1. Иди на https://render.com
2. Зарегистрируйся (можно через GitHub)
3. Подтверди email

#### Шаг 5: Создай PostgreSQL базу данных

1. В Render Dashboard → **New +** → **PostgreSQL**
2. Заполни:
   - Name: `news-insight-db`
   - Database: `news_insight`
   - User: `news_insight_user`
   - Region: выбери ближайший
   - Plan: **Free** (для теста) или **Starter** (для продакшена)
3. Нажми **Create Database**
4. **Скопируй Internal Database URL** - понадобится!

#### Шаг 6: Создай Web Service

1. В Render Dashboard → **New +** → **Web Service**
2. Подключи GitHub репозиторий
3. Заполни:
   - **Name:** `news-insight-parser`
   - **Region:** тот же что и БД
   - **Branch:** `main`
   - **Root Directory:** (оставь пустым)
   - **Runtime:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `gunicorn --bind 0.0.0.0:$PORT --timeout 600 --workers 2 --threads 4 --worker-class gthread app_v2:app`
   - **Plan:** Free (для теста) или Starter (для продакшена)

#### Шаг 7: Установи переменные окружения

В разделе **Environment** добавь:

**ОБЯЗАТЕЛЬНЫЕ:**
```
DATABASE_URL = (вставь Internal Database URL из шага 5)
GROQ_API_KEY = твой_ключ_от_groq
```

**ДЛЯ TELEGRAM (опционально):**
```
TELEGRAM_BOT_TOKEN = 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHANNEL_ID = @your_channel_name
```

**ДЛЯ АВТОМАТИЗАЦИИ:**
```
AUTO_GENERATE_ENABLED = true
AUTO_GENERATE_HOUR = 9
AUTO_GENERATE_MINUTE = 0
TOPIC_EXCLUDE_DAYS = 30
CONTENT_LANGUAGE = ru
CONTENT_FORMAT = long_post
CONTENT_TONE = professional
```

#### Шаг 8: Деплой!

Нажми **Create Web Service**

Render начнёт деплой (займёт 2-5 минут).

#### Шаг 9: Запусти миграцию БД

Когда деплой завершится:

1. Иди в **Shell** (вкладка в сервисе)
2. Выполни:
```bash
python migrate_add_used_topics.py
```

Это создаст таблицу `used_topics`.

#### Шаг 10: Запусти парсеры

```bash
curl -X POST https://твой-app.onrender.com/api/parse
```

Или открой в браузере страницу приложения и нажми "Parse Now".

#### Шаг 11: Запусти аналитику

```bash
curl -X POST https://твой-app.onrender.com/api/run-insights
```

#### Шаг 12: Проверь автоматизацию

```bash
# Проверь статус:
curl https://твой-app.onrender.com/api/automation-status

# Попробуй вручную:
curl -X POST https://твой-app.onrender.com/api/auto-generate
```

Если вернулся `content_id` и `message_id` - **УСПЕХ!** Контент в Telegram канале.

#### Шаг 13: Жди следующего дня в 9:00

Автоматизация запустится сама и опубликует пост в Telegram!

---

## 📊 Чек-лист: Что должно быть

### Минимум для работы локально:
- [ ] GROQ_API_KEY установлен
- [ ] Приложение запущено (`python app_v2.py`)
- [ ] Парсеры запустились (есть посты в БД)
- [ ] Аналитика запустилась (есть темы в БД)

### Для продакшена на Render:
- [ ] GitHub репозиторий создан
- [ ] PostgreSQL база создана на Render
- [ ] Web Service создан на Render
- [ ] DATABASE_URL установлен
- [ ] GROQ_API_KEY установлен
- [ ] Миграция БД выполнена
- [ ] Парсеры запущены
- [ ] Аналитика запущена

### Для автопостинга в Telegram:
- [ ] Telegram бот создан (@BotFather)
- [ ] Telegram канал создан
- [ ] Бот добавлен админом в канал
- [ ] TELEGRAM_BOT_TOKEN установлен
- [ ] TELEGRAM_CHANNEL_ID установлен
- [ ] AUTO_GENERATE_ENABLED=true установлен

---

## 🚨 Частые проблемы

### "AutoContentSystem not configured"
**Причина:** Нет GROQ_API_KEY
**Решение:** Установи переменную окружения

### "No suitable topic found"
**Причина:** Нет тем в БД
**Решение:** Запусти парсеры, потом аналитику

### "Database connection failed"
**Причина:** Неверный DATABASE_URL
**Решение:** Скопируй правильный URL из Render Dashboard

### "Telegram posting failed"
**Причина:** Бот не админ канала
**Решение:** Добавь бота как администратора

---

## ⏱️ Сколько времени займёт?

- **Локальный тест:** 5-10 минут
- **Деплой на Render (без Telegram):** 15-20 минут
- **Деплой на Render (с Telegram):** 25-30 минут

---

## 📞 Если что-то не работает

1. Проверь логи в Render Dashboard
2. Проверь статус: `curl https://твой-app.onrender.com/api/automation-status`
3. Проверь переменные окружения в Render
4. Проверь что миграция БД выполнена

---

## 🎉 Когда всё работает

Увидишь:
- ✅ В логах: `[INFO] SCHEDULED CONTENT GENERATION STARTED`
- ✅ В Telegram канале: новый пост с картинкой
- ✅ В БД: новый контент в `generated_content`
- ✅ Темы не повторяются

**Система работает полностью автоматически!**

---

## 📚 Полезные ссылки

- **Groq API ключ:** https://console.groq.com/keys
- **Render:** https://render.com
- **Telegram Bot создание:** @BotFather в Telegram
- **Подробный гайд:** DEPLOYMENT_GUIDE.md
- **Тестирование:** TESTING_GUIDE_RU.md
