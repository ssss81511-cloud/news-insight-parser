# Wake-and-Run System 🚀

Умная система пробуждения Render сервера перед парсингом и постингом.

## Как работает система

### 1. GitHub Actions Workflow
Запускается **3 раза в день** в 8:55, 13:55, 19:55 UTC:
- За 5 минут до запланированного парсинга/постинга (9:00, 14:00, 20:00)
- Пингует endpoint `/api/wake-and-run`
- **Retry логика**: До 10 попыток каждые 30 секунд
- Максимальное время ожидания: 5 минут

### 2. Endpoint `/api/wake-and-run`
Проверяет пропущенные задачи:
- **Парсинг**: Если прошло >6 часов с последнего парсинга → запускает парсинг
- **Автопостинг**: Если пропущено запланированное время → запускает постинг
- Логирует все действия
- Возвращает список запущенных задач

### 3. Startup Check
При запуске сервера (после пробуждения от сна):
- Проверяет когда был последний парсинг
- Если >6 часов → автоматически запускает парсинг через 10 секунд
- Сервер готов к работе сразу после пробуждения

### 4. Упрощенное расписание
Все источники парсятся **каждые 8 часов** (3 раза в день):
- Hacker News, Reddit, TechCrunch, Dev.to: каждые 8 часов
- Product Hunt, VC Blogs: 1 раз в день (обновляются редко)

## Настройка

### 1. Environment Variables на Render
Убедитесь что настроены переменные для автопостинга:
```bash
# Обязательные
GROQ_API_KEY=your_key_here
TELEGRAM_BOT_TOKEN=your_token
TELEGRAM_CHANNEL_ID=@your_channel

# Автопостинг (3 раза в день)
AUTO_GENERATE_ENABLED=true
AUTO_GENERATE_HOURS=9,14,20  # UTC время
AUTO_GENERATE_MINUTE=0

# Опциональные
PEXELS_API_KEY=your_pexels_key  # Fallback для картинок
```

### 2. GitHub Actions
Workflow уже настроен в `.github/workflows/keepalive.yml`

**Важно**: Убедитесь что в настройках репозитория:
- Settings → Actions → General
- ✅ "Allow all actions and reusable workflows"
- ✅ Workflow permissions: "Read repository contents..."

### 3. Включить автопарсинг
После деплоя на Render, откройте:
```
https://news-insight-parser.onrender.com
```

Нажмите **"Enable Auto-Parsing"** для нужных источников.

## Тестирование

### 1. Протестировать wake-and-run endpoint локально
```bash
curl -X POST http://localhost:5001/api/wake-and-run
```

Ответ покажет какие задачи были запущены:
```json
{
  "status": "success",
  "message": "Wake-and-run completed",
  "tasks_triggered": ["parsing"],
  "timestamp": "2025-12-22T14:00:00Z"
}
```

### 2. Протестировать на Render
```bash
curl -X POST https://news-insight-parser.onrender.com/api/wake-and-run
```

### 3. Запустить GitHub Actions вручную
1. Перейдите на GitHub: `Actions` → `Wake and Run Parser`
2. Нажмите `Run workflow` → `Run workflow`
3. Смотрите логи выполнения

### 4. Проверить логи Render
В Render Dashboard → Logs смотрите за:
```
[WAKE-AND-RUN: Endpoint called]
[STARTUP] Checking for missed tasks...
[PARSER] Starting to parse all sources
```

## Расписание (UTC)

| Время | Действие |
|-------|----------|
| 8:55  | GitHub Actions пингует wake-and-run |
| 9:00  | Парсинг + Автопостинг (утро) |
| 13:55 | GitHub Actions пингует wake-and-run |
| 14:00 | Парсинг + Автопостинг (день) |
| 19:55 | GitHub Actions пингует wake-and-run |
| 20:00 | Парсинг + Автопостинг (вечер) |

**Примечание**: Если ваш часовой пояс отличается от UTC, конвертируйте время:
- UTC 9:00 = MSK 12:00 (Москва)
- UTC 14:00 = MSK 17:00
- UTC 20:00 = MSK 23:00

## Monitoring

### Проверить статус автоматизации
```bash
curl https://news-insight-parser.onrender.com/api/automation-status
```

### Проверить статус парсера
```bash
curl https://news-insight-parser.onrender.com/api/scheduler/status
```

### Проверить статистику автопостинга
```bash
curl https://news-insight-parser.onrender.com/api/auto-stats
```

## Troubleshooting

### Сервер все равно засыпает?
1. **Проверьте GitHub Actions запускаются**:
   - Перейдите в GitHub → Actions
   - Должны быть запуски каждые ~5 часов

2. **Проверьте логи workflow**:
   - Если "❌ Failed to wake server" → возможно Render down
   - Если "✅ Wake-and-run completed" → все работает

3. **Проверьте логи Render**:
   - Ищите "[WAKE-AND-RUN]" в логах
   - Если нет → GitHub Actions не доходит до сервера

### Парсинг не запускается?
1. Проверьте что автопарсинг включен:
   ```bash
   curl https://news-insight-parser.onrender.com/api/scheduler/status
   ```
   Должно быть: `"auto_parse_enabled": true`

2. Проверьте когда был последний парсинг:
   ```bash
   curl https://news-insight-parser.onrender.com/api/status | grep last_parse
   ```

3. Запустите wake-and-run вручную:
   ```bash
   curl -X POST https://news-insight-parser.onrender.com/api/wake-and-run
   ```

### Автопостинг не работает?
1. Проверьте environment variables на Render
2. Проверьте логи автоматизации:
   ```bash
   curl https://news-insight-parser.onrender.com/api/auto-stats
   ```
3. Запустите вручную:
   ```bash
   curl -X POST https://news-insight-parser.onrender.com/api/auto-generate
   ```

## Преимущества новой системы

✅ **Надежность**: Retry логика гарантирует пробуждение сервера
✅ **Простота**: Все происходит автоматически 3 раза в день
✅ **Экономия**: Render free tier не достигает лимитов
✅ **Логирование**: Полная прозрачность всех операций
✅ **Fallback**: Если GitHub Actions не сработает, есть interval-based парсинг
✅ **Startup recovery**: Сервер автоматически догоняет пропущенные задачи

## Логи для отладки

Полезные логи для мониторинга:
```
[WAKE-AND-RUN: Endpoint called]
[WAKE-AND-RUN: Last parse was X hours ago]
[WAKE-AND-RUN: Parsing is overdue -> triggering parsing]
[WAKE-AND-RUN: Tasks triggered: ['parsing', 'auto_posting']]
[STARTUP] Checking for missed tasks...
[STARTUP] Starting delayed parsing...
```
