# 📊 КАК ОТСЛЕЖИВАТЬ РАБОТУ СИСТЕМЫ

Полный гайд по мониторингу автоматизации.

---

## 🎯 3 СПОСОБА МОНИТОРИНГА:

1. **Web-интерфейс** (в браузере)
2. **API endpoints** (через curl/Postman)
3. **Логи** (консоль/файлы)

---

## 1️⃣ WEB-ИНТЕРФЕЙС (Самый простой)

### Открой в браузере:

**Главная страница:**
```
http://localhost:5001
```

Здесь увидишь:
- Кнопку "Parse Now" - запустить парсеры
- Кнопку "Run Insights" - запустить аналитику
- Статистику системы
- Последние посты

**Страница аналитики:**
```
http://localhost:5001/analytics
```

Здесь увидишь:
- Все найденные темы
- Количество постов в каждой теме
- Ключевые слова
- Важность темы

**Сгенерированный контент:**
```
http://localhost:5001/generated
```

Здесь увидишь:
- Весь сгенерированный контент
- Когда создан
- Опубликован ли в Telegram
- Можно почитать полный текст

---

## 2️⃣ API ENDPOINTS (Для программистов)

### Базовая информация

**Статус системы:**
```bash
curl http://localhost:5001/
```

**Проверить что сервер работает:**
```bash
curl http://localhost:5001/health
```

### Мониторинг автоматизации

**Статус автоматизации:**
```bash
curl http://localhost:5001/api/automation-status | python -m json.tool
```

Покажет:
```json
{
  "enabled": true,           // Включена ли автоматизация
  "configured": true,        // Настроена ли система
  "telegram_enabled": true,  // Настроен ли Telegram
  "schedule": "09:00",       // Время запуска
  "jobs": [                  // Запланированные задачи
    {
      "id": "daily_content_generation",
      "next_run": "2025-12-21T09:00:00..."
    }
  ]
}
```

**Статистика работы:**
```bash
curl http://localhost:5001/api/auto-stats | python -m json.tool
```

Покажет:
```json
{
  "status": "success",
  "stats": {
    "total_generated": 42,      // Всего сгенерировано
    "total_published": 38,      // Всего опубликовано
    "topics_used_30d": 25,      // Использовано тем за 30 дней
    "topics_per_week": 5.2,     // Тем в неделю
    "publish_rate": 90,         // % публикации
    "last_run": "2025-12-20T15:30:00"  // Последний запуск
  }
}
```

### Данные

**Сколько постов в БД:**
```bash
curl http://localhost:5001/api/posts/count
```

**Последние посты:**
```bash
curl http://localhost:5001/api/posts/recent?limit=10
```

**Темы из аналитики:**
```bash
curl http://localhost:5001/api/topics
```

**Сгенерированный контент:**
```bash
curl http://localhost:5001/api/generated-content | python -m json.tool
```

Покажет массив:
```json
[
  {
    "id": 1,
    "title": "Заголовок поста",
    "content": "Полный текст...",
    "format": "long_post",
    "language": "ru",
    "created_at": "2025-12-20T15:30:00",
    "published": true,
    "telegram_message_id": 123,
    "hashtags": ["#AI", "#Tech"]
  }
]
```

### Действия

**Запустить парсеры:**
```bash
curl -X POST http://localhost:5001/api/parse
```

**Запустить аналитику:**
```bash
curl -X POST http://localhost:5001/api/run-insights
```

**Сгенерировать контент вручную:**
```bash
curl -X POST http://localhost:5001/api/auto-generate | python -m json.tool
```

**Включить автоматизацию:**
```bash
curl -X POST http://localhost:5001/api/automation/enable
```

**Выключить автоматизацию:**
```bash
curl -X POST http://localhost:5001/api/automation/disable
```

---

## 3️⃣ ЛОГИ (Детальная информация)

### Где смотреть логи

**Во время работы приложения:**
```bash
python app_v2.py
```

Увидишь в консоли:
```
[INFO] SCHEDULED CONTENT GENERATION STARTED
[INFO] [STEP 1/7] Selecting unique topic...
[INFO] [OK] Selected topic: ['AI', 'automation', 'LLM']
[INFO] [STEP 2/7] Fetching posts...
[INFO] [OK] Found 5 posts
[INFO] [STEP 3/7] Generating content...
[INFO] [OK] Content generated: 1234 chars
[INFO] [STEP 4/7] Saving content to database...
[INFO] [OK] Content saved with ID: 42
[INFO] [STEP 5/7] Marking topic as used...
[INFO] [OK] Topic marked as used
[INFO] [STEP 6/7] Generating reel image...
[INFO] [OK] Reel generated: generated_reels/reel_20251220_123456.jpg
[INFO] [STEP 7/7] Posting to Telegram...
[INFO] [OK] Posted to Telegram: Message ID 567
[INFO] SUCCESS - Content ID: 42, Message ID: 567
```

**После запуска (в фоне):**

Логи пишутся в stdout, можешь перенаправить:
```bash
python app_v2.py > logs/app.log 2>&1 &
```

Потом смотреть:
```bash
tail -f logs/app.log
```

### Что искать в логах

**Успешная генерация:**
```
[INFO] SUCCESS - Content ID: X, Message ID: Y
```

**Ошибки:**
```
[ERROR] Content generation failed: ...
[ERROR] Telegram posting failed: ...
[WARNING] Reel generation failed: ...
```

**Автоматизация:**
```
[INFO] SCHEDULED CONTENT GENERATION STARTED
```

---

## 4️⃣ БАЗА ДАННЫХ (Прямой доступ)

### SQLite (локально)

```bash
sqlite3 data/techcrunch.db
```

**Полезные запросы:**

```sql
-- Сколько постов
SELECT COUNT(*) FROM posts;

-- Сколько тем
SELECT COUNT(*) FROM topics;

-- Сколько сгенерировано
SELECT COUNT(*) FROM generated_content;

-- Последние сгенерированные
SELECT id, title, created_at, published
FROM generated_content
ORDER BY created_at DESC
LIMIT 10;

-- Использованные темы
SELECT keywords, used_at, content_id
FROM used_topics
ORDER BY used_at DESC
LIMIT 10;

-- Статистика
SELECT
  (SELECT COUNT(*) FROM posts) as posts,
  (SELECT COUNT(*) FROM topics) as topics,
  (SELECT COUNT(*) FROM generated_content) as generated,
  (SELECT COUNT(*) FROM used_topics) as used_topics;
```

### PostgreSQL (на Render)

Подключись через psql:
```bash
psql postgresql://user:pass@host/db
```

Те же запросы работают.

---

## 5️⃣ ПРОВЕРИТЬ ВСЁ СРАЗУ (Чек-лист)

```bash
# 1. Сервер работает?
curl http://localhost:5001/health

# 2. Есть посты?
curl http://localhost:5001/api/posts/count

# 3. Есть темы?
curl http://localhost:5001/api/topics | python -m json.tool

# 4. Автоматизация настроена?
curl http://localhost:5001/api/automation-status | python -m json.tool

# 5. Есть сгенерированный контент?
curl http://localhost:5001/api/generated-content | python -m json.tool

# 6. Статистика
curl http://localhost:5001/api/auto-stats | python -m json.tool
```

---

## 6️⃣ TELEGRAM (Проверить публикацию)

**Через API Telegram:**

```bash
# Твои данные
BOT_TOKEN="8568827955:AAFagsX78hhOuWUEafW7DedgT64oRMhAH3Q"
CHANNEL_ID="@newsinsigth"

# Проверить что бот работает
curl https://api.telegram.org/bot${BOT_TOKEN}/getMe

# Проверить права в канале
curl https://api.telegram.org/bot${BOT_TOKEN}/getChatMember?chat_id=${CHANNEL_ID}&user_id=8568827955

# Последнее сообщение в канале
curl https://api.telegram.org/bot${BOT_TOKEN}/getUpdates?limit=1
```

**Через канал:**

Просто открой: https://t.me/newsinsigth

Там должны быть посты!

---

## 7️⃣ МОНИТОРИНГ В РЕАЛЬНОМ ВРЕМЕНИ

### Dashboard в терминале (watch)

```bash
# Обновляется каждые 2 секунды
watch -n 2 'curl -s http://localhost:5001/api/auto-stats | python -m json.tool'
```

### Логи в реальном времени

```bash
# Следить за логами
python app_v2.py 2>&1 | grep -E "(SUCCESS|ERROR|STEP)"
```

---

## 8️⃣ АЛЕРТЫ И УВЕДОМЛЕНИЯ

### Настроить уведомления об ошибках

Добавь в конец `app_v2.py`:

```python
import logging
from logging.handlers import SMTPHandler

# Email уведомления при ошибках
mail_handler = SMTPHandler(
    mailhost=('smtp.gmail.com', 587),
    fromaddr='your@gmail.com',
    toaddrs=['admin@example.com'],
    subject='[ERROR] News Insight Parser',
    credentials=('your@gmail.com', 'password'),
    secure=()
)
mail_handler.setLevel(logging.ERROR)
logger.addHandler(mail_handler)
```

Теперь при ошибках будут приходить email!

---

## 9️⃣ МЕТРИКИ ДЛЯ ОТСЛЕЖИВАНИЯ

**Каждый день проверяй:**
- ✅ Сколько постов собрано сегодня
- ✅ Создалась ли новая тема
- ✅ Сгенерировался ли контент в 9:00
- ✅ Опубликовался ли пост в Telegram

**Каждую неделю:**
- 📊 Сколько тем использовано
- 📊 Процент успешной публикации
- 📊 Средняя длина контента
- 📊 Повторялись ли темы

**Каждый месяц:**
- 📈 Рост базы данных
- 📈 Качество генерируемого контента
- 📈 Вовлеченность в Telegram

---

## 🔟 БЫСТРАЯ ДИАГНОСТИКА

**Проблема: Контент не генерируется**

```bash
# 1. Проверь что есть посты
curl http://localhost:5001/api/posts/count

# 2. Проверь что есть темы
curl http://localhost:5001/api/topics

# 3. Проверь GROQ API
echo $GROQ_API_KEY

# 4. Попробуй вручную
curl -X POST http://localhost:5001/api/auto-generate
```

**Проблема: Не постится в Telegram**

```bash
# 1. Проверь токен
curl https://api.telegram.org/bot8568827955:AAFagsX78hhOuWUEafW7DedgT64oRMhAH3Q/getMe

# 2. Проверь права
curl https://api.telegram.org/bot8568827955:AAFagsX78hhOuWUEafW7DedgT64oRMhAH3Q/getChatMember?chat_id=@newsinsigth&user_id=8568827955

# 3. Проверь настройки
curl http://localhost:5001/api/automation-status
```

**Проблема: Автоматизация не запускается**

```bash
# 1. Проверь что включена
curl http://localhost:5001/api/automation-status

# 2. Включи если выключена
curl -X POST http://localhost:5001/api/automation/enable

# 3. Проверь логи
grep "SCHEDULED CONTENT" logs/app.log
```

---

## ✅ ЧЕКЛИСТ "ВСЁ РАБОТАЕТ"

- [ ] Сервер отвечает на http://localhost:5001
- [ ] В БД есть посты (>100)
- [ ] В БД есть темы (>10)
- [ ] automation-status показывает enabled: true
- [ ] Есть хотя бы 1 сгенерированный контент
- [ ] Telegram бот подключен
- [ ] В канале @newsinsigth есть пост
- [ ] Логи не показывают ERROR
- [ ] auto-stats показывает publish_rate > 80%

**Если все галочки - СИСТЕМА РАБОТАЕТ ИДЕАЛЬНО!** 🎉

---

## 📚 КОМАНДЫ ДЛЯ КОПИРОВАНИЯ

Сохрани эти команды для быстрого доступа:

```bash
# Проверка статуса
alias check-status='curl -s http://localhost:5001/api/automation-status | python -m json.tool'

# Статистика
alias check-stats='curl -s http://localhost:5001/api/auto-stats | python -m json.tool'

# Сгенерировать контент
alias generate-now='curl -X POST http://localhost:5001/api/auto-generate | python -m json.tool'

# Посмотреть логи
alias check-logs='tail -100 logs/app.log | grep -E "(SUCCESS|ERROR|WARNING)"'

# Включить автоматизацию
alias enable-auto='curl -X POST http://localhost:5001/api/automation/enable'

# Выключить автоматизацию
alias disable-auto='curl -X POST http://localhost:5001/api/automation/disable'
```

Добавь в `.bashrc` или `.zshrc` для постоянного использования!

---

**ТЕПЕРЬ ТЫ ЗНАЕШЬ КАК ОТСЛЕЖИВАТЬ ВСЁ!** 🚀
