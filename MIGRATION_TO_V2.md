# Миграция на версию 2.0 - Завершена!

Все 3 шага выполнены. Система готова к использованию.

---

## ✅ Что было сделано:

### 1. ✅ Рефакторинг HNParser

**Создан:** `parsers/hacker_news/refactored_parser.py`

**Изменения:**
- Теперь наследует от `BaseParser`
- Использует унифицированные методы:
  - `fetch_posts(section, limit)`
  - `fetch_comments(post_id, limit)`
  - `normalize_post(raw_post)` → UniversalPost формат
  - `normalize_comment(raw_comment, post_db_id)` → UniversalComment формат

**Преимущества:**
- Совместим с новой архитектурой
- Автоматическая генерация content_hash
- Автоматический расчёт importance_score
- Готов к работе с дедупликацией

---

### 2. ✅ ParserOrchestrator создан

**Создан:** `parsers/orchestrator.py`

**Функционал:**
```python
orchestrator = ParserOrchestrator(db)

# Зарегистрировать парсеры
orchestrator.register_parser(hn_parser)
orchestrator.register_parser(reddit_parser)  # В будущем

# Парсить один источник
orchestrator.parse_source('hacker_news', sections=['ask_hn', 'show_hn'])

# Парсить все источники
orchestrator.parse_all(limit_per_section=20)

# Статус
status = orchestrator.get_status()
```

**Преимущества:**
- Централизованное управление
- Параллельный парсинг (опционально)
- Обработка ошибок
- Единый интерфейс для всех источников

---

### 3. ✅ Веб-интерфейс обновлён

**Создано:**
- `app_v2.py` - новая версия Flask приложения
- `templates/index_v2.html` - главная страница v2
- `templates/posts_v2.html` - посты с UniversalPost
- `templates/signals_v2.html` - сигналы с приоритетами и контекстом

**Новые возможности:**

#### На главной странице:
- ✅ Статистика critical/trending сигналов
- ✅ Кнопка "Анализировать сигналы" - запуск детектора из UI
- ✅ Информация о новой архитектуре

#### На странице постов:
- ✅ importance_score для каждого поста (⭐ 85/100)
- ✅ Индикатор дубликатов (🔗 Дубликат обнаружен)
- ✅ Показ источника (hacker_news, reddit, etc.)

#### На странице сигналов:
- ✅ Приоритеты: 🔴 Critical, 🟠 High, 🟡 Medium, ⚪ Low
- ✅ Кросс-источниковые сигналы (HN + Reddit)
- ✅ Реальные цитаты (context snippets)
- ✅ Trending индикатор (🔥)
- ✅ Показ роста (📈 Растёт / 📉 Падает)

---

## 🚀 Как запустить новую версию:

### Шаг 1: Остановить старую версию

Если старый app.py запущен на порту 5000:
```bash
# В терминале где запущен app.py, нажать Ctrl+C
```

Или через ID:
```bash
# Найти ID процесса
# Использовать KillShell если нужно
```

### Шаг 2: Запустить новую версию

```bash
python app_v2.py
```

**Новая версия запустится на порту 5001** (чтобы не конфликтовать со старой)

### Шаг 3: Открыть браузер

```
http://localhost:5001
```

---

## 📊 Новые возможности в UI:

### 1. Запуск анализа сигналов из интерфейса

**Раньше:** Только через Python код
```python
from storage.database import DatabaseManager
from analyzers.signal_detector import SignalDetector
db = DatabaseManager()
detector = SignalDetector(db)
detector.detect_all_signals()
```

**Теперь:** Кнопка в UI!
- Зайти на главную страницу
- Нажать "Анализировать сигналы"
- Готово!

---

### 2. Фильтры по приоритетам

```
/signals                     → Все сигналы
/signals?priority=critical   → Только критические
/signals?priority=high       → Только важные
/signals?trending=true       → Только trending
```

---

### 3. Контекст в сигналах

**Раньше:**
```
Signal: "pricing problem"
Frequency: 10
```

**Теперь:**
```
Signal: "pricing problem"
Priority: 🔴 Critical
Importance: ⭐ 85/100
Frequency: 15 упоминаний
Sources: hacker_news, reddit

💬 Примеры:
"...I'm struggling with pricing my SaaS. Hard to find the right model..."
"...customers complain pricing is too complicated. Need to simplify..."
"...switched from monthly to annual pricing. Revenue up 40%..."

📈 Растёт
```

---

## 🔄 Миграция данных

### Важно понять:

**Старая БД (models.py):**
- `hn_posts` - только HN
- `hn_comments` - только HN
- `signals` - простые сигналы

**Новая БД (universal_models.py):**
- `universal_posts` - все источники
- `universal_comments` - все источники
- `enhanced_signals` - сигналы с приоритетами
- `duplicate_groups` - дедупликация

### Два варианта:

#### Вариант A: Начать с чистой БД (рекомендуется)

```bash
# Сделать бэкап старой БД
cp data/insights.db data/insights_old.db

# Удалить старую БД
rm data/insights.db

# Запустить app_v2.py - создаст новую БД
python app_v2.py

# Запустить парсинг заново
```

**Плюсы:** Чистая новая архитектура, нет проблем
**Минусы:** Потеряете старые данные (но они есть в бэкапе)

---

#### Вариант B: Мигрировать старые данные (сложнее)

Если хотите сохранить старые данные, нужно создать скрипт миграции:

```python
# migrate_to_v2.py
from storage.database import DatabaseManager  # Старый
from storage.universal_database import UniversalDatabaseManager  # Новый
from storage.models import HNPost, HNComment
from storage.universal_models import UniversalPost, UniversalComment

old_db = DatabaseManager()
new_db = UniversalDatabaseManager()

# Мигрировать посты
old_posts = old_db.session.query(HNPost).all()
for old_post in old_posts:
    new_post_data = {
        'source': 'hacker_news',
        'source_id': str(old_post.hn_id),
        'source_url': f"https://news.ycombinator.com/item?id={old_post.hn_id}",
        'title': old_post.title,
        'content': old_post.text,
        'author': old_post.author,
        'score': old_post.score,
        'comments_count': old_post.comments_count,
        'post_type': old_post.post_type,
        'created_at': old_post.created_at,
    }
    new_db.add_universal_post(new_post_data)

# Аналогично для комментариев
```

---

## 🎯 Рекомендация:

**Начните с чистой БД (Вариант A)**

Почему:
1. Быстрее (5 минут vs 1 час на миграцию)
2. Гарантированно работает
3. Старые данные сохранены в бэкапе
4. Новый парсинг с новой архитектурой соберёт актуальные данные

**Если нужны старые данные:**
- Они в `data/insights_old.db`
- Можно открыть через DB Browser for SQLite
- Можно экспортировать в CSV
- Можно написать скрипт миграции позже

---

## 📝 Тестирование новой версии:

### 1. Запустить app_v2.py
```bash
python app_v2.py
```

### 2. Открыть http://localhost:5001

### 3. Проверить главную страницу
- ✅ Видна информация о v2.0
- ✅ Есть кнопка "Анализировать сигналы"
- ✅ Статистика показывает 0 постов (новая БД)

### 4. Запустить парсинг
- Нажать "Запустить парсинг"
- Подождать 2-3 минуты
- Обновить страницу

### 5. Проверить посты
- Перейти на /posts
- ✅ Посты отображаются
- ✅ Видны importance_score
- ✅ Видны источники

### 6. Запустить анализ сигналов
- Вернуться на главную
- Нажать "Анализировать сигналы"
- Подождать ~30 секунд

### 7. Проверить сигналы
- Перейти на /signals
- ✅ Сигналы с приоритетами
- ✅ Видны context snippets
- ✅ Видны тренды

---

## 🆕 API Endpoints v2:

### POST /api/parse
Запустить парсинг

**Тело запроса:**
```json
{
  "sources": ["hacker_news"],  // Или null для всех
  "limit": 20
}
```

### POST /api/analyze-signals
Запустить анализ сигналов

**Тело запроса:**
```json
{
  "lookback_days": 7,
  "min_mentions": 3
}
```

### GET /api/status
Получить статус

**Ответ:**
```json
{
  "parser_status": {
    "is_running": false,
    "last_run": "2025-12-17T10:00:00"
  },
  "orchestrator_status": {
    "registered_sources": ["hacker_news"],
    "current_source": null
  },
  "stats": {
    "total_posts": 120,
    "critical_signals": 5,
    "trending_signals": 3
  }
}
```

---

## 🔜 Следующие шаги:

После успешного запуска v2:

1. ✅ **Протестировать все функции**
2. ✅ **Запустить парсинг несколько раз**
3. ✅ **Проверить дедупликацию** (когда добавится второй источник)
4. ✅ **Настроить автоматический парсинг** (cron/scheduler)
5. 🔜 **Добавить Reddit парсер**
6. 🔜 **Добавить Product Hunt парсер**

---

## 📁 Структура файлов v2:

```
parser/
├── app_v2.py                              # Новое Flask приложение
├── parsers/
│   ├── base_parser.py                     # Базовый класс ✅
│   ├── orchestrator.py                    # Оркестратор ✅
│   └── hacker_news/
│       ├── parser.py                      # Старый парсер
│       └── refactored_parser.py           # Новый парсер ✅
├── storage/
│   ├── models.py                          # Старые модели
│   ├── database.py                        # Старый менеджер
│   ├── universal_models.py                # Новые модели ✅
│   └── universal_database.py              # Новый менеджер ✅
├── analyzers/
│   ├── signal_detector.py                 # Старый детектор
│   └── enhanced_signal_detector.py        # Новый детектор ✅
├── templates/
│   ├── index.html                         # Старая главная
│   ├── index_v2.html                      # Новая главная ✅
│   ├── posts_v2.html                      # Новые посты ✅
│   └── signals_v2.html                    # Новые сигналы ✅
└── data/
    └── insights.db                        # БД (будет пересоздана)
```

---

## ❓ FAQ:

### Q: Можно ли использовать старую и новую версию одновременно?

**A:** Да, они на разных портах:
- Старая: http://localhost:5000
- Новая: http://localhost:5001

Но используют одну БД, поэтому лучше выбрать одну.

---

### Q: Что делать со старым app.py?

**A:** Оставить как есть. Когда v2 будет стабильна:
```bash
mv app.py app_v1_backup.py
mv app_v2.py app.py
```

---

### Q: Нужно ли переустанавливать зависимости?

**A:** Нет, все зависимости те же самые.

---

### Q: Как добавить новый источник теперь?

**A:** Создать класс, наследующий BaseParser:

```python
from parsers.base_parser import BaseParser

class RedditParser(BaseParser):
    def __init__(self):
        super().__init__('reddit')

    def fetch_posts(self, section, limit):
        # Получить посты из Reddit
        return posts

    def normalize_post(self, raw_post):
        # Преобразовать Reddit → Universal формат
        return {
            'source': 'reddit',
            'source_id': raw_post['id'],
            'title': raw_post['title'],
            'content': raw_post['selftext'],
            'score': raw_post['ups'],
            # ...
        }
```

Зарегистрировать в оркестраторе:
```python
reddit_parser = RedditParser()
orchestrator.register_parser(reddit_parser)
```

Готово! Всё остальное (дедупликация, приоритизация, контекст) работает автоматически.

---

## 🎉 Итог:

✅ Унифицированная архитектура
✅ Дедупликация
✅ Приоритизация
✅ Контекст
✅ Веб-интерфейс
✅ Готово к расширению!

**Система полностью готова к добавлению Reddit, Product Hunt и других источников!**
