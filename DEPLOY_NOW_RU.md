# 🚀 ДЕПЛОЙ НА RENDER - Пошаговая инструкция

## ✅ Что уже готово:

- ✅ Код на GitHub: https://github.com/ssss81511-cloud/news-insight-parser
- ✅ render.yaml настроен
- ✅ Все файлы автоматизации готовы
- ✅ Документация написана

---

## 📋 ЧТО ТЕБЕ НУЖНО СДЕЛАТЬ СЕЙЧАС:

### Шаг 1: Зарегистрируйся на Render (если ещё нет)

1. Иди на https://render.com
2. Нажми **Get Started** или **Sign Up**
3. Зарегистрируйся через GitHub (рекомендуется) или email
4. Подтверди email если нужно
5. Войди в Render Dashboard: https://dashboard.render.com

---

### Шаг 2: Создай Blueprint (автоматический деплой)

Это создаст ВСЁ автоматически из render.yaml!

1. В Render Dashboard нажми **New +** (синяя кнопка справа сверху)

2. Выбери **Blueprint**

3. Подключи GitHub репозиторий:
   - Если первый раз: нажми **Connect GitHub**
   - Разреши Render доступ к твоим репозиториям
   - Найди `news-insight-parser` в списке
   - Нажми **Connect**

4. Render обнаружит `render.yaml` и покажет что создаст:
   ```
   Services to create:
   - news-insight-parser (Web Service)
   - news-insight-db (PostgreSQL Database)
   ```

5. Нажми **Apply**

Render начнёт создавать:
- PostgreSQL базу данных
- Web Service
- Установит все зависимости
- Запустит приложение

**ПОДОЖДИ 5-10 минут**, пока деплой завершится.

---

### Шаг 3: Настрой переменные окружения (ВАЖНО!)

После того как деплой завершится:

#### 3.1 Добавь GROQ_API_KEY (ОБЯЗАТЕЛЬНО)

1. В Dashboard иди в **news-insight-parser** (Web Service)
2. Слева выбери вкладку **Environment**
3. Найди переменную `GROQ_API_KEY`
4. Нажми на неё
5. Вставь твой ключ от Groq
6. Нажми **Save Changes**

#### 3.2 Добавь Telegram токены (если хочешь автопостинг)

В той же вкладке **Environment**:

1. Найди `TELEGRAM_BOT_TOKEN`
   - Вставь токен от @BotFather (например: `123456789:ABCdefGHIjklMNO...`)
   - Save

2. Найди `TELEGRAM_CHANNEL_ID`
   - Вставь ID канала (например: `@your_channel` или `-100xxxxxxxxxx`)
   - Save

Render автоматически перезапустится с новыми переменными.

---

### Шаг 4: Запусти миграцию базы данных

После того как приложение перезапустилось:

1. В news-insight-parser сервисе иди на вкладку **Shell**

2. Подожди пока Shell загрузится (появится `$` prompt)

3. Выполни команду:
```bash
python migrate_add_used_topics.py
```

4. Должно появиться:
```
Creating used_topics table...
Migration completed successfully!
```

✅ База данных готова!

---

### Шаг 5: Запусти парсеры

Теперь нужно собрать первые посты:

#### Вариант A: Через Shell

В Shell выполни:
```bash
curl -X POST http://localhost:10000/api/parse
```

#### Вариант B: Через браузер

1. Скопируй URL твоего приложения (например: `https://news-insight-parser.onrender.com`)
2. Открой в браузере: `https://твой-app.onrender.com`
3. Нажми кнопку **Parse Now**

Парсинг займёт 2-5 минут. Следи за логами во вкладке **Logs**.

---

### Шаг 6: Запусти аналитику

После того как парсинг завершился:

#### Вариант A: Через терминал

```bash
curl -X POST https://твой-app.onrender.com/api/run-insights
```

#### Вариант B: Через браузер

Открой: `https://твой-app.onrender.com`

Найди кнопку **Run Insights** и нажми.

Аналитика займёт 30-60 секунд.

---

### Шаг 7: Проверь автоматизацию

Теперь попробуем сгенерировать пост:

```bash
curl -X POST https://твой-app.onrender.com/api/auto-generate
```

**Если всё ОК, увидишь:**
```json
{
  "status": "success",
  "content_id": 1,
  "message_id": 123,  // Если Telegram настроен
  "topic": {
    "keywords": ["AI", "automation", "LLM"],
    "post_count": 5
  }
}
```

**Если Telegram настроен - зайди в свой канал!** Там должен быть новый пост с картинкой! 🎉

---

### Шаг 8: Проверь что автоматизация включена

```bash
curl https://твой-app.onrender.com/api/automation-status
```

Должно быть:
```json
{
  "enabled": true,  // ← Включена!
  "configured": true,
  "telegram_enabled": true,  // Если настроил Telegram
  "jobs": [
    {
      "id": "daily_content_generation",
      "next_run": "2025-12-21T09:00:00..."  // Следующий запуск
    }
  ]
}
```

---

## 🎉 ГОТОВО! Автоматизация работает!

Система теперь будет:
- ✅ Каждый день в 9:00 выбирать уникальную тему
- ✅ Генерировать контент на русском
- ✅ Создавать красивую картинку
- ✅ Постить в Telegram канал (если настроен)
- ✅ Не повторять темы 30 дней

---

## 📊 Мониторинг

### Смотреть логи:

В Render Dashboard → news-insight-parser → **Logs**

Ищи сообщения:
```
[INFO] SCHEDULED CONTENT GENERATION STARTED
[INFO] [STEP 1/7] Selecting unique topic...
[INFO] [OK] Selected topic: ['AI', 'automation']
...
[INFO] SUCCESS - Content ID: 42, Message ID: 567
```

### Проверить статистику:

```bash
curl https://твой-app.onrender.com/api/auto-stats
```

---

## ⚙️ Полезные команды

```bash
# Проверить статус
curl https://твой-app.onrender.com/api/automation-status

# Посмотреть сгенерированный контент
curl https://твой-app.onrender.com/api/generated-content

# Вручную сгенерировать пост
curl -X POST https://твой-app.onrender.com/api/auto-generate

# Включить автоматизацию (если была выключена)
curl -X POST https://твой-app.onrender.com/api/automation/enable

# Выключить автоматизацию
curl -X POST https://твой-app.onrender.com/api/automation/disable
```

---

## 🚨 Если что-то не работает

### "No suitable topic found"

**Причина:** Нет тем в базе

**Решение:**
1. Запусти парсеры: `/api/parse`
2. Запусти аналитику: `/api/run-insights`
3. Попробуй снова: `/api/auto-generate`

### "AutoContentSystem not configured"

**Причина:** Нет GROQ_API_KEY

**Решение:**
1. Environment → GROQ_API_KEY
2. Вставь ключ
3. Save Changes

### "Telegram posting failed"

**Причина:** Бот не админ в канале или неверный токен

**Решение:**
1. Проверь что бот добавлен как администратор
2. Проверь TELEGRAM_BOT_TOKEN правильный
3. Проверь TELEGRAM_CHANNEL_ID формат (@channel или -100xxx)

### База данных не работает

**Причина:** Не запущена миграция

**Решение:**
1. Shell → `python migrate_add_used_topics.py`

---

## 📞 Нужна помощь?

1. Проверь логи в Render Dashboard
2. Проверь что все переменные окружения установлены
3. Проверь что миграция БД выполнена
4. Проверь что парсеры и аналитика отработали

---

## 🔑 Где взять Telegram токен (если ещё нет)

1. Открой Telegram
2. Найди @BotFather
3. Отправь: `/newbot`
4. Назови бота: `My Content Bot`
5. Username: `my_content_bot`
6. Получишь токен: `123456789:ABCdef...`
7. Создай канал в Telegram
8. Добавь бота как администратора канала
9. ID канала: `@твой_канал` (для публичного) или узнай через @getmyid_bot (для приватного)

---

## ✅ Чек-лист "Всё ли готово?"

- [ ] Render аккаунт создан
- [ ] Blueprint применён (оба сервиса созданы)
- [ ] GROQ_API_KEY установлен
- [ ] TELEGRAM_BOT_TOKEN установлен (опционально)
- [ ] TELEGRAM_CHANNEL_ID установлен (опционально)
- [ ] Миграция БД выполнена
- [ ] Парсеры запущены (есть посты)
- [ ] Аналитика запущена (есть темы)
- [ ] Тестовая генерация сработала
- [ ] Пост появился в Telegram (если настроен)
- [ ] `automation-status` показывает `enabled: true`

**Если все галочки стоят - ПОЗДРАВЛЯЮ! 🎉**

Система полностью автоматизирована и работает!

Каждое утро в 9:00 будет новый пост в твоём канале! 🚀
