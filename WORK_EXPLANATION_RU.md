# Как работает парсер - Полное объяснение

## 📊 Архитектура системы (последовательно)

### Шаг 1: Источник данных - Hacker News API

**Что используется:**
- Официальный Firebase API от Hacker News
- URL: `https://hacker-news.firebaseio.com/v0/`
- Не требует регистрации или API ключей
- Бесплатный и стабильный

**Какие разделы парсятся:**
1. **Ask HN** (`/askstories`) - вопросы от основателей и разработчиков
2. **Show HN** (`/showstories`) - новые продукты и запросы на фидбэк
3. **New** (`/newstories`) - самые свежие истории

---

### Шаг 2: Как работает парсинг (алгоритм)

#### Когда вы нажимаете "Start Parsing":

**1. Запуск парсера (app.py:92)**
```
run_parser() - функция запускается в отдельном потоке
└─> Парсер работает в фоне, не блокируя веб-интерфейс
```

**2. Последовательность сбора данных:**

```
Ask HN (20 постов)
  ├─> Получить список ID постов из /askstories
  ├─> Для каждого ID:
  │   ├─> Получить пост (название, текст, автор, счёт)
  │   ├─> Получить первые 10 комментариев
  │   └─> Сохранить в БД
  └─> Записать статистику запуска

Show HN (20 постов)
  ├─> Получить список ID из /showstories
  ├─> Для каждого ID:
  │   ├─> Получить пост
  │   ├─> Получить первые 10 комментариев
  │   └─> Сохранить в БД
  └─> Записать статистику

New Stories (20 постов)
  ├─> Получить список ID из /newstories
  ├─> Для каждого ID:
  │   ├─> Получить пост
  │   └─> Сохранить в БД (без комментариев для скорости)
  └─> Записать статистику
```

**3. Rate Limiting (ограничение скорости):**
- Между каждым запросом к API задержка **1 секунда**
- Это предотвращает блокировку со стороны HN
- Общее время парсинга: ~2-3 минуты для 60 постов

---

### Шаг 3: Хранение данных

**База данных: SQLite**
- Расположение: `data/insights.db`
- Автоматически создаётся при первом запуске
- Легковесная, не требует сервера

**Таблицы:**

#### 1. `hn_posts` - Посты
| Поле | Описание | Пример |
|------|----------|--------|
| `hn_id` | ID поста на HN | 38749123 |
| `title` | Заголовок | "Ask HN: How to find first customers?" |
| `text` | Текст поста | "I'm building a SaaS..." |
| `author` | Автор | "pg" |
| `score` | Очки (upvotes) | 42 |
| `post_type` | Тип | "ask_hn" / "show_hn" / "new" |
| `created_at` | Когда создан | 2025-12-17 10:30 |
| `fetched_at` | Когда спарсили | 2025-12-17 10:36 |
| `comments_count` | Кол-во комментариев | 15 |

#### 2. `hn_comments` - Комментарии
| Поле | Описание |
|------|----------|
| `hn_id` | ID комментария |
| `post_id` | К какому посту относится |
| `text` | Текст комментария |
| `author` | Автор |

#### 3. `signals` - Обнаруженные сигналы
| Поле | Описание |
|------|----------|
| `signal_type` | "pain" / "language" / "solution" |
| `title` | Название сигнала |
| `description` | Описание |
| `frequency` | Сколько раз встречается |
| `keywords` | Ключевые слова |

#### 4. `parser_runs` - История запусков
| Поле | Описание |
|------|----------|
| `source` | "hacker_news" |
| `section` | "ask_hn" / "show_hn" / "new" |
| `items_fetched` | Сколько собрано |
| `status` | "success" / "failed" |
| `started_at` | Время запуска |

**Важно:** Данные хранятся **постоянно** и **накапливаются**!
- При повторном запуске парсер **обновляет** существующие посты
- Новые посты **добавляются**
- Данные **НЕ удаляются** автоматически

---

### Шаг 4: Как работают Signals (детектирование паттернов)

**Важно:** Сейчас signals **НЕ запускаются автоматически**!

Чтобы обнаружить сигналы, нужно:

#### Вариант 1: Запустить вручную через Python
```python
from storage.database import DatabaseManager
from analyzers.signal_detector import SignalDetector

db = DatabaseManager()
detector = SignalDetector(db)
detector.detect_all_signals(lookback_days=7)
```

#### Вариант 2: Добавить в веб-интерфейс (TODO)

**Как работает детектор сигналов:**

##### 1. Repeating Pains (Повторяющиеся проблемы)
```
1. Берёт все посты и комментарии за последние 7 дней
2. Ищет ключевые слова боли: "problem", "struggle", "difficult", etc.
3. Извлекает контекст вокруг этих слов
4. Считает частоту упоминаний
5. Если слово упоминается >= 3 раз → создаёт сигнал
```

**Пример:**
```
Ключевое слово: "pricing"
Частота: 5 упоминаний
Сигнал: "Repeating pain: pricing"
Описание: "Mentioned 5 times. Common pain point among founders."
```

##### 2. Emerging Language (Новые термины)
```
1. Извлекает все слова из постов
2. Фильтрует технические термины (с заглавных, содержат "-", цифры)
3. Считает частоту
4. Если >= 5 упоминаний → новый термин
```

**Пример:**
```
Термин: "AI-agents"
Частота: 8 упоминаний
Сигнал: "Emerging term: AI-agents"
```

##### 3. Solution Patterns (Паттерны решений)
```
1. Ищет посты со словами: "built", "created", "solution", "workaround"
2. Извлекает ключевые слова из контекста
3. Находит общие темы
4. Если >= 2 упоминаний → паттерн решения
```

**Минимальные пороги (можно настроить в config/settings.py):**
- `min_pain_mentions: 3` - минимум упоминаний для боли
- `min_pattern_occurrences: 2` - минимум для паттерна
- `lookback_days: 30` - сколько дней анализировать

---

## 🔄 Правильный рабочий процесс

### День 1 - Первый запуск
1. ✅ Запустить парсер → собрать первые ~60 постов
2. ✅ Изучить собранные данные в разделе Posts
3. ⏳ Signals пока пусто (мало данных)

### День 2-7 - Накопление данных
1. Запускать парсер **1-2 раза в день**
2. Данные накапливаются в БД
3. После ~200-300 постов можно запустить детектор сигналов

### После недели - Анализ
1. Запустить детектор сигналов
2. Изучить обнаруженные паттерны
3. Найти повторяющиеся проблемы и тренды

---

## 📈 Рекомендации по использованию

### Частота парсинга:
- **Оптимально:** 2-3 раза в день (утро, обед, вечер)
- **Минимум:** 1 раз в день
- **Не рекомендуется:** каждый час (много дублей)

### Почему?
- Новые Ask HN появляются не так часто
- Show HN обновляется медленно
- Парсинг каждый час = много дублирующихся данных

### Объём данных:
- **1 запуск** = ~60 постов + ~200 комментариев
- **За неделю (2 запуска/день)** = ~840 постов + ~2800 комментариев
- **За месяц** = ~3600 постов + ~12000 комментариев

### Размер базы данных:
- **100 постов** ≈ 100-200 KB
- **1000 постов** ≈ 1-2 MB
- **10000 постов** ≈ 10-20 MB

**Вывод:** БД растёт медленно, места занимает мало.

---

## 💾 Хранение и резервное копирование

### Где хранятся данные:
```
parser/
└─ data/
   └─ insights.db  ← ВСЕ данные здесь
```

### Резервное копирование:
```bash
# Просто скопировать файл
cp data/insights.db data/insights_backup_2025-12-17.db

# Или автоматизировать
python -c "import shutil; from datetime import datetime; shutil.copy('data/insights.db', f'data/backup_{datetime.now().strftime(\"%Y%m%d\")}.db')"
```

### Очистка старых данных:
Если БД стала слишком большой:
```python
from storage.database import DatabaseManager
from datetime import datetime, timedelta

db = DatabaseManager()

# Удалить посты старше 90 дней
cutoff = datetime.utcnow() - timedelta(days=90)
old_posts = db.session.query(HNPost).filter(HNPost.created_at < cutoff).delete()
db.session.commit()
```

---

## 🎯 Что дальше улучшить

1. **Автоматический запуск** - добавить Schedule для регулярного парсинга
2. **Signals в UI** - кнопка для запуска детектора сигналов
3. **Экспорт данных** - выгрузка в CSV/JSON
4. **Дашборд статистики** - графики по времени
5. **Email/Telegram уведомления** - при обнаружении новых сигналов

---

## 🐛 Возможные проблемы

### "No posts found"
- Парсер ещё не запускался
- Нажмите "Start Parsing"

### "Signals пусто"
- Детектор сигналов не запускался
- Мало данных (< 100 постов)
- Запустите вручную через Python

### Парсер работает долго
- Нормально! 60 постов × 1 сек задержка = ~2-3 минуты
- Можно уменьшить `rate_limit_delay` в parser.py (риск блокировки)

### БД заблокирована
- SQLite не поддерживает одновременную запись
- Подождите окончания текущего парсинга
