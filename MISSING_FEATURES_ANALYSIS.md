# Анализ недостающих функций - Последовательное мышление

## 🔍 Шаг 1: Текущее состояние (что уже есть)

✅ **Работает:**
- Парсер Hacker News (Ask HN, Show HN, New)
- База данных SQLite с моделями
- Веб-интерфейс на Flask
- Детектор сигналов (но не интегрирован)
- Хранение постов и комментариев
- Базовая статистика

---

## ❌ Шаг 2: Критические недостатки (надо исправить СРОЧНО)

### 1. Signals не интегрированы в UI
**Проблема:**
- Детектор сигналов работает только через Python код
- Пользователь не может запустить анализ из веб-интерфейса
- Signals не обновляются автоматически

**Что нужно:**
```
✅ Кнопка "Анализировать сигналы" на дашборде
✅ Автоматический запуск после парсинга
✅ Настройка параметров (lookback_days, min_mentions)
```

---

### 2. Нет автоматизации парсинга
**Проблема:**
- Парсер запускается только вручную
- Нужно постоянно заходить и жать кнопку
- Данные не собираются регулярно

**Что нужно:**
```
✅ Планировщик задач (schedule, cron)
✅ Настройка интервалов (каждый час/день)
✅ Возможность включить/выключить автопарсинг
```

---

### 3. Нет экспорта данных
**Проблема:**
- Данные заперты в БД
- Нельзя выгрузить для анализа в Excel/Python
- Нет интеграции с другими инструментами

**Что нужно:**
```
✅ Экспорт в CSV/JSON
✅ API endpoints для получения данных
✅ Экспорт сигналов в отчёт
```

---

### 4. Нет поиска и фильтрации
**Проблема:**
- Нельзя найти пост по ключевому слову
- Нет фильтра по дате
- Нет сортировки по score/комментариям

**Что нужно:**
```
✅ Полнотекстовый поиск по постам
✅ Фильтры: дата, автор, score, тип
✅ Сортировка по разным параметрам
```

---

## ⚠️ Шаг 3: Важные недостатки (нужно добавить)

### 5. Нет дедупликации
**Проблема:**
- При повторном парсинге могут быть дубли
- Один и тот же пост может обновляться (score меняется)
- Нет отслеживания изменений

**Что нужно:**
```
✅ Проверка дубликатов по hn_id
✅ Обновление score/комментариев существующих постов
✅ История изменений (было 10 upvotes → стало 50)
```

---

### 6. Нет визуализации данных
**Проблема:**
- Только таблицы и цифры
- Нет графиков динамики
- Сложно видеть тренды

**Что нужно:**
```
✅ График: посты по времени
✅ График: топ ключевых слов
✅ Тренды упоминаний проблем
✅ Активность по дням недели
```

---

### 7. Нет приоритизации сигналов
**Проблема:**
- Все сигналы равнозначны
- Нельзя отметить важные
- Нет "горячих" трендов

**Что нужно:**
```
✅ Scoring сигналов (важность)
✅ Категории: критичный, важный, интересный
✅ Фильтр по важности
✅ "Горячие" сигналы (растут быстро)
```

---

### 8. Нет уведомлений
**Проблема:**
- Нужно постоянно заходить проверять
- Можно пропустить важный сигнал
- Нет алертов

**Что нужно:**
```
✅ Email уведомления о новых сигналах
✅ Telegram бот
✅ Webhook для интеграций
```

---

## 🏗️ Шаг 4: Архитектурные проблемы (ВАЖНО перед расширением!)

### 9. Нет единого интерфейса парсеров
**Проблема:**
- HN парсер - отдельный класс
- Когда добавите Reddit, Product Hunt - будут отдельные классы
- Нет общей логики

**Что нужно СРОЧНО:**
```python
# Абстрактный базовый класс
class BaseParser:
    def parse(self):
        pass

    def normalize_data(self):
        pass

    def save_to_db(self):
        pass

# Все парсеры наследуют
class HNParser(BaseParser):
    ...

class RedditParser(BaseParser):
    ...
```

---

### 10. Нет нормализации данных из разных источников
**Проблема:**
- HN: title, text, score
- Reddit: title, selftext, upvotes
- Product Hunt: name, description, votes

**Одни и те же вещи, разные названия!**

**Что нужно:**
```python
# Единая модель для всех источников
class UnifiedPost:
    source: str  # 'hacker_news', 'reddit', 'product_hunt'
    source_id: str  # ID в источнике
    title: str
    content: str  # text, selftext, description → все в content
    author: str
    score: int  # score, upvotes, votes → все в score
    url: str
    created_at: datetime
    post_type: str  # ask, show, discussion, launch
```

---

### 11. Нет кросс-источникового анализа
**Проблема:**
- Signals детектируются отдельно для HN
- Когда добавите Reddit - будут отдельные signals
- Но одна и та же проблема может обсуждаться на HN И Reddit!

**Что нужно:**
```
✅ Объединение похожих сигналов из разных источников
✅ "Pricing problems" на HN + "Pricing issues" на Reddit = 1 сигнал
✅ Подсчёт общей частоты по всем источникам
```

---

### 12. Нет централизованного оркестратора
**Проблема:**
- Сейчас кнопка запускает HN парсер
- Когда будет 5 источников - 5 кнопок?
- Или 1 кнопка парсит всё подряд 30 минут?

**Что нужно:**
```python
class ParserOrchestrator:
    def __init__(self):
        self.parsers = {
            'hacker_news': HNParser(),
            'reddit': RedditParser(),
            'product_hunt': PHParser(),
        }

    def parse_all(self):
        # Параллельно парсит все источники
        pass

    def parse_source(self, source_name):
        # Парсит один источник
        pass
```

---

## 📊 Шаг 5: UX проблемы

### 13. Нет трендов во времени
**Проблема:**
- Видим только snapshot сейчас
- Не видим "pricing упоминается всё чаще"
- Нет исторической динамики

**Что нужно:**
```
✅ График: частота упоминаний ключевого слова по дням
✅ "Растущие тренды" - что обсуждают больше
✅ "Затухающие тренды" - о чём перестали говорить
```

---

### 14. Нет сохранённых поисков
**Проблема:**
- Если интересует конкретная тема (например, "AI pricing")
- Нужно каждый раз искать заново

**Что нужно:**
```
✅ Сохранённые поиски
✅ Подписка на ключевые слова
✅ Алерты при новых упоминаниях
```

---

### 15. Нет пагинации
**Проблема:**
- Когда будет 10000 постов
- Страница /posts будет грузиться вечность
- Браузер может зависнуть

**Что нужно:**
```
✅ Пагинация (по 50 постов)
✅ Бесконечная прокрутка
✅ Фильтр "показать больше"
```

---

## 🔧 Шаг 6: Технические улучшения

### 16. Слабое логирование
**Проблема:**
- Ошибки только в консоли
- Нет истории ошибок
- Сложно дебажить

**Что нужно:**
```
✅ Логирование в файл (logs/parser.log)
✅ Уровни: DEBUG, INFO, WARNING, ERROR
✅ Ротация логов
```

---

### 17. Нет индексов в БД
**Проблема:**
- При большом объёме данных запросы будут медленные
- Нет индексов на часто используемых полях

**Что нужно:**
```sql
CREATE INDEX idx_post_type ON hn_posts(post_type);
CREATE INDEX idx_created_at ON hn_posts(created_at);
CREATE INDEX idx_fetched_at ON hn_posts(fetched_at);
CREATE INDEX idx_signal_type ON signals(signal_type);
```

---

### 18. Нет обработки ошибок
**Проблема:**
- Если HN API недоступен - парсер падает
- Нет retry логики
- Весь парсинг останавливается из-за 1 ошибки

**Что нужно:**
```python
try:
    post = fetch_post(id)
except RequestException:
    # Retry 3 раза
    # Если не получилось - пропустить, но продолжить
    pass
```

---

## 🎯 Шаг 7: ПРИОРИТЕТЫ перед расширением

### Критически важно сделать СЕЙЧАС:

#### 1. Базовая архитектура для мульти-источников
```python
# parsers/base_parser.py
class BaseParser(ABC):
    @abstractmethod
    def fetch_posts(self):
        pass

    @abstractmethod
    def normalize_post(self, raw_post):
        pass
```

#### 2. Унифицированная модель данных
```python
# storage/models.py
class UniversalPost(Base):
    source = Column(String)  # 'hn', 'reddit', 'ph'
    source_id = Column(String)
    title = Column(String)
    content = Column(Text)
    # ... остальное
```

#### 3. Интеграция Signals в UI
```html
<button onclick="runSignalDetection()">
    Анализировать сигналы
</button>
```

#### 4. Базовый поиск
```python
@app.route('/search')
def search():
    query = request.args.get('q')
    posts = db.search_posts(query)
    return render_template('search.html', posts=posts)
```

#### 5. Экспорт данных
```python
@app.route('/api/export/signals')
def export_signals():
    signals = db.get_all_signals()
    return jsonify(signals)
```

---

## 📋 Рекомендуемый порядок действий

### До добавления новых источников:

**Неделя 1: Архитектура**
1. ✅ Создать BaseParser
2. ✅ Создать UniversalPost модель
3. ✅ Рефакторить HNParser под новую архитектуру
4. ✅ Создать ParserOrchestrator

**Неделя 2: Ключевые фичи**
5. ✅ Интегрировать Signals в UI
6. ✅ Добавить поиск
7. ✅ Добавить экспорт
8. ✅ Добавить пагинацию

**Неделя 3: Улучшения**
9. ✅ Автоматизация парсинга (scheduler)
10. ✅ Обработка ошибок
11. ✅ Логирование
12. ✅ Индексы БД

**Неделя 4: Расширение**
13. ✅ Добавить Reddit парсер
14. ✅ Добавить Product Hunt парсер
15. ✅ Кросс-источниковый анализ

---

## 💡 Самые важные вещи, которые можно упустить:

### 1. Дедупликация между источниками
Один и тот же продукт может быть на HN, Reddit, Product Hunt.
Нужно определять: это один и тот же объект или разные?

### 2. Качество vs количество сигналов
100 слабых сигналов хуже чем 5 сильных.
Нужна система оценки важности.

### 3. Temporal analysis
Не просто "сколько раз упоминается", а "растёт или падает".
Тренд важнее абсолютного числа.

### 4. Context preservation
Сигнал "pricing problem" - это хорошо.
Но ЧТО ИМЕННО не нравится в pricing? Контекст важен.

### 5. False positives
Слово "problem" в "I solved the problem" ≠ боль.
Нужен sentiment analysis или контекстный анализ.

---

## 🚀 Краткий вывод

**Что КРИТИЧНО сделать перед расширением:**
1. Унифицированная архитектура парсеров (BaseParser)
2. Общая модель данных (UniversalPost)
3. Интеграция Signals в UI
4. Базовый поиск и экспорт

**Что можно отложить:**
- Уведомления
- Продвинутая визуализация
- Telegram бот
- Saved searches

**Самый большой риск:**
Добавить 5 источников без унифицированной архитектуры →
Получить 5 несовместимых систем →
Потом переписывать всё с нуля.

---

## Следующий шаг?

Хотите:
1. Сначала сделать базовую архитектуру?
2. Или сразу добавить Reddit/Product Hunt, а рефакторить потом?
3. Или сфокусироваться на улучшении текущего HN парсера?
